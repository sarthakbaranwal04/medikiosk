"""
Security utilities (password hashing, JWT creation/validation).

Placeholder for Phase 1 — the full implementation (password hashing with
passlib, JWT issuance/verification, role-based dependency helpers) is added
in Phase 2 alongside the User model and auth router, so it can be tested
end-to-end rather than left unused.
"""
